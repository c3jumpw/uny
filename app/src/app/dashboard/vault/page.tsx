import { StubPage } from "@/components/StubPage";

export default function VaultPage() {
  return (
    <StubPage
      title="Vault"
      description="Store credentials and secrets used by your connected services."
    />
  );
}

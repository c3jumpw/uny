import { StubPage } from "@/components/StubPage";

export default function DomainsPage() {
  return (
    <StubPage
      title="Domains"
      description="Connect and manage the domains that point at your UnyBase workspace."
    />
  );
}

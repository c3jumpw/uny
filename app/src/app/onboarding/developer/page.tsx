import { StubPage } from "@/components/StubPage";

export default function DeveloperOnboardingPage() {
  return (
    <StubPage
      title="Set up as a developer"
      description="We'll walk you through connecting your app to UnyBase."
    />
  );
}
